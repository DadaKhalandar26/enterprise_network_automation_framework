# logs/

Runtime logs written by services and playbook runs.

The **contents are gitignored** — only this README and a `.gitkeep` are tracked so the
directory exists on clone. Do not commit log files.
