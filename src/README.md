# src/

Python source, organized as an installable package under `netauto/`. The `src/` layout
keeps imports clean and ensures tests run against the installed package rather than
stray local files.

See [`netauto/README.md`](netauto/README.md) for the package internals.
