# utils/

Small, boring, reusable helpers used across the package.

- Logging setup so every service logs consistently.
- Validators (e.g. IP address / prefix checks).
- Output formatters.

Keep this dependency-light and free of business logic.
